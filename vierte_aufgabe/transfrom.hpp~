#IFNDEF TRANSFORM_H
#DEFINE TRANSFORM_H

#include <vector>
#include <algorithm>
#include <cmath>

using namespace std;

vector<int> vec_to_int(vector<double> dubs);
vector<double> vec_rounded(vector<double> unrounded);

#ENDIF
