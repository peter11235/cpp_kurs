#include <iostream>
#include "date.hpp"

using namespace std;

string to_string(Date d; bool numbered_months)
{
    string res = "empty";
    if (b)
    {
            res = d.day + "." + d.month + "." + d.year;
    }
    else
    {
        switch(d.month)
        {
            case 1:
                res = d.day + ". Januar " + d.year;
            case 2:
                res = d.day + ". Februar " + d.year;
            case 3:
                res = d.day + ". März " + d.year;
            case 4:
                res = d.day + ". April " + d.year;
            case 5:
                res = d.day + ". Mai " + d.year;
            case 6:
                res = d.day + ". Juni " + d.year;
            case 7:
                res = d.day + ". Juli " + d.year;
            case 8:
                res = d.day + ". August " + d.year;
            case 9:
                res = d.day + ". September " + d.year;
            case 10:
                res = d.day + ". Oktober " + d.year;
            case 11:
                res = d.day + ". November " + d.year;
            case 12:
                res = d.day + ". Dezember " + d.year;
        }
    }
    return res;
}
